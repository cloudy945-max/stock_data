TUSHARE_TOKEN=your_tushare_token_here
AKShare_ENABLED=true
TUSHARE_ENABLED=true

DB_PATH=/path/to/stock_data/data/stock_data.duckdb
DATA_DIR=/path/to/stock_data/data

# ARM NAS 优化配置
MAX_CONCURRENT=2
REQUEST_DELAY_MIN=2.5
REQUEST_DELAY_MAX=6.0
MAX_RETRIES=5

# ARM 模式配置
PYTHONARM_MODE=true
DUCKDB_THREADS=1
DB_MEMORY_LIMIT=2GB
CONNECTION_POOL_SIZE=4

# 财务数据更新频率 (daily/weekly/monthly)
FINANCIAL_UPDATE_FREQ=weekly
FINANCIAL_WEEKLY_DAY=5

# 邮件通知配置
MAIL_ENABLED=false
MAIL_SMTP_SERVER=smtp.qq.com
MAIL_SMTP_PORT=465
MAIL_USERNAME=your_email@example.com
MAIL_PASSWORD=your_email_password
MAIL_RECEIVERS=["receiver1@example.com"]

# 代理配置
PROXY_ENABLED=false
PROXY_URL=http://proxy:port

# 更新控制
UPDATE_DAILY=true
UPDATE_VALUATION=true
UPDATE_FINANCIAL=true
